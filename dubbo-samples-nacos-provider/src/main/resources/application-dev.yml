# redis配置
spring:
  redis:
    host: localhost
    port: 6379
    password:
# dubbo配置
dubbo:
  application:
    name: provider
    qos-enable: true
    qos-port: 33333
    qos-accept-foreign-ip: false
  protocol:
    name: dubbo
    port: -1
  registry:
    address: nacos://localhost:8848
